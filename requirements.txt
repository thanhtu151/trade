# VN Stock trading platform — base deps (Python 3.10), pinned to the versions
# running on the dev machine. Used by BOTH the Streamlit Cloud dashboard and the
# GitHub Actions scheduler. Heavy TensorFlow/Keras live in requirements-ml.txt so
# the Streamlit build stays light (dashboard lazy-loads TF only for the LSTM tab).
# The scheduler installs both files.
#
# If a pin fails to resolve a Linux wheel on CI, loosen just that line.

# --- core data ---
pandas==2.3.3
numpy==2.2.6
scipy==1.15.3
pyarrow==24.0.0
requests==2.34.2
python-dotenv==1.0.1

# --- market data sources ---
vnstock==4.0.4
vnai==2.4.8
yfinance==1.4.1
finnhub-python==2.4.28
feedparser==6.0.12
beautifulsoup4==4.15.0
soupsieve==2.8.4
lxml==6.1.1

# --- ML (tabular ensemble; TensorFlow/Keras are in requirements-ml.txt) ---
scikit-learn==1.7.2
xgboost==3.2.0
lightgbm==4.6.0
joblib==1.5.3

# --- UI / dashboard ---
streamlit==1.58.0
plotly==6.8.0
altair==6.2.1

# --- scheduling / process ---
schedule==1.2.2
psutil==7.2.2
